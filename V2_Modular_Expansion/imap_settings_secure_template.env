# IMAP credentials stored securely as environment variables
IMAP_HOST=imap.example.com
IMAP_PORT=993
IMAP_SSL=true
IMAP_USER=materials-intake@example.com
IMAP_PASS=${IMAP_PASSWORD}  # Use a secret store or docker secret in deployment

# Recommendation:
# - Load this with dotenv or secure environment variable injection
# - Avoid hardcoding credentials in plain text configs