FROM activepieces/activepieces:latest

# ActivePieces runs as non-root user by default
# No need to switch users or install additional packages
# The base image already includes everything needed

# The base image already sets up the working directory and exposes port 8080
# No additional configuration needed
