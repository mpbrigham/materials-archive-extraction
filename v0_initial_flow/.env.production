# IMIS V1 Environment Configuration Template - IMAP Email Ingestion
# Non-sensitive configuration for production server
# Sensitive credentials (EMAIL_USER, EMAIL_PASS, LLM_API_KEY) are injected from GitHub Secrets

# Email Configuration (non-sensitive)
IMAP_HOST='imap.hostinger.com'
IMAP_PORT=993
SMTP_HOST='smtp.hostinger.com'
SMTP_PORT=465

# LLM API Configuration (non-sensitive)
LLM_API_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

# Note: Sensitive values are injected at deployment time:
# - EMAIL_USER (from GitHub secret)
# - EMAIL_PASS (from GitHub secret) 
# - EMAIL_USER_TEST (from GitHub secret)
# - LLM_API_KEY (from GitHub secret)
