# Materials Intake Pipeline Environment Configuration - Langflow
# Copy this file to .env and fill in ALL required values
# THERE ARE NO DEFAULTS - ALL VALUES MUST BE CONFIGURED

# Email Configuration (REQUIRED)
IMAP_HOST=        # Your IMAP server hostname (e.g., mail.example.com)
IMAP_PORT=        # Your IMAP port (typically 993 for SSL/TLS)
SMTP_HOST=        # Your SMTP server hostname (e.g., mail.example.com)
SMTP_PORT=        # Your SMTP port (typically 587 for STARTTLS or 465 for SSL)
EMAIL_USER=       # Your email address
EMAIL_PASS=       # Your email password

# LLM Configuration (REQUIRED)
LLM_API_KEY=      # Your Google Gemini API key
LLM_MODEL=        # Model name (e.g., gemini-2.0-flash)

# Security Configuration (REQUIRED for production)
LANGFLOW_SECRET_KEY=      # Secret key for encryption (generate with: python3 -c "from secrets import token_urlsafe; print(token_urlsafe(32))")
LANGFLOW_AUTO_LOGIN=      # Set to 'false' for production (default: true)
LANGFLOW_SUPERUSER=       # Admin username (required if AUTO_LOGIN is false)
LANGFLOW_SUPERUSER_PASSWORD=  # Admin password (required if AUTO_LOGIN is false)

# Network Configuration (REQUIRED)
VPN_IP=           # Your VPN IP address for accessing Langflow UI
